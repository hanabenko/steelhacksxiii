import { chromium } from '@playwright/test';
const browser=await chromium.launch({channel:'chrome',args:['--enable-unsafe-swiftshader']});const page=await browser.newPage({viewport:{width:1440,height:1050}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:5173');await page.locator('#pause').click();await page.screenshot({path:'artifacts/free-mode.png'});
await page.locator('#play-mode').click();await page.locator('#design-panel').evaluate(el=>Promise.all(el.getAnimations().map(a=>a.finished)));await page.screenshot({path:'artifacts/game-challenge.png'});
await page.setViewportSize({width:390,height:844});await page.screenshot({path:'artifacts/game-mobile.png'});await page.locator('#exit-game').click();await page.locator('#quick-run').click();await page.locator('#simulation-panel').evaluate(el=>Promise.all(el.getAnimations().map(a=>a.finished)));await page.screenshot({path:'artifacts/free-settings-mobile.png'});console.log(JSON.stringify({errors}));await browser.close();
