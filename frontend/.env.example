# Optional full Python/SUMO POST endpoint. Blank uses the explicit local prototype.
# Backend must allow the frontend origin through CORS. Never put secrets in VITE_*.
VITE_SIMULATION_API_URL=
# Optional simulation-owned contract endpoint for the Forbes × Bigelow SUMO replay.
# It may point at the same service as VITE_SIMULATION_API_URL.
VITE_SIMULATION_BASELINE_URL=
